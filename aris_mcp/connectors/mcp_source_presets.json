{
  "_comment": "Tier-1 mcp_tool source preset(s) for aris-mcp (AU-KG.ingest.mcp-tool-connector). Declarative server+tool+field map so the KG syncs ARIS models with no agent-utilities code. Contributed presets win over the central MCP_TOOL_PRESETS.",
  "aris-models": {
    "server": "aris-mcp",
    "tool": "aris_model",
    "action": "list",
    "params": { "action": "list", "params_json": "{}" },
    "records_path": "",
    "id_field": "guid",
    "id_fallback_fields": ["id", "modelGuid", "ID"],
    "title_field": "name",
    "text_field": "name",
    "type_field": "type",
    "updated_field": "lastChange",
    "doc_type": "ProcessModel",
    "node_id_template": "aris:model:{guid}",
    "domain": "aris"
  }
}
